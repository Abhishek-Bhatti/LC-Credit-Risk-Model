# LC-Credit-Risk-Model
This repo contains a model to assess creditworthiness in borrowers based on applicant profiles and lending appetite of the lender. It is trained on historical lending data from LendingClub, and uses a combined response from both an XGBoost classifier and a Llama 3.3 (via Groq LPU).
